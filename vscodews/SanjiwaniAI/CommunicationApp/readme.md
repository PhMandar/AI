### To create virtual environment run following command:
python3 -m venv venv

### To activate virtual environment run following command:
venv\Scripts\activate

### To install project requirements
pip install -r requirements.tx

### To run program, you need to give following command
python run.py